replicaCount: 1
image:
  repository: nginx
  tag: latest
  pullPolicy: IfNotPresent
service:
  type: ClusterIP
  port: 80
customerName: "Default Customer"
elasticSearch:
  host: "changeme"
  apiKey: "changeme"
